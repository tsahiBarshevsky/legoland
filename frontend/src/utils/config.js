import {
    API_KEY,
    AUTH_DOMAIN,
    PROJECT_ID,
    STORAGE_BUCKET,
    MESSANING_SENDER_ID,
    APP_ID,
    PUBLISHABLE_KEY
} from "@env";

export default {
    API_KEY,
    AUTH_DOMAIN,
    PROJECT_ID,
    STORAGE_BUCKET,
    MESSANING_SENDER_ID,
    APP_ID,
    PUBLISHABLE_KEY
};